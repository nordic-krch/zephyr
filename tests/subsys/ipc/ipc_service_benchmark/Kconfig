
source "Kconfig.zephyr"

config TEST_MULTI_ENDPOINT
	bool
	default y if IPC_SERVICE_BACKEND_MPSC
	default y if IPC_SERVICE_BACKEND_ICBMSG

config TEST_NO_COPY
	bool
	default y if IPC_SERVICE_BACKEND_MPSC_NO_COPY
